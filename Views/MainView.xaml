<UserControl x:Class="StockControl.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
             
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0"
                    HorizontalAlignment="Stretch"
                    Margin="0,0,0,20">

            <TextBlock Text="StockControl"
                    FontSize="24"
                    FontWeight="Bold"/>

            <TextBlock Text="{Binding Title}"
                    Margin="20,0,0,0"/>

        </StackPanel>

        <!-- Welcome -->
        <TextBlock Grid.Row="1"
                Text="Bienvenido al panel de control de StockControl"
                FontSize="20"
                FontWeight="SemiBold"
                Margin="0,0,0,20"/>

        <!-- Dashboard -->
        <UniformGrid Grid.Row="2" Columns="2" Rows="2">

            <Border Margin="10" Padding="15" Background="#EEE">
                <TextBlock Text="Proximamente"/>
            </Border>

            <Border Margin="10" Padding="15" Background="#EEE">
                <TextBlock Text="Proximamente"
                           Visibility="{Binding ViewMode,
                                            Converter={StaticResource ModeToVisibilityConverter},
                                            ConverterParameter=AdminOnly}"/>
            </Border>

            <Border Margin="10" Padding="15" Background="#EEE">
                <TextBlock Text="Proximamente"/>
            </Border>

            <Border Margin="10" Padding="15" Background="#EEE">
                <TextBlock Text="Proximamente"
                           Visibility="{Binding ViewMode,
                                            Converter={StaticResource ModeToVisibilityConverter},
                                            ConverterParameter=AdminOnly}"/>
            </Border>

        </UniformGrid>

    </Grid>
</UserControl>